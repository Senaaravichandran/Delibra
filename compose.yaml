services:
  delibra:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DELIBRA_ENVIRONMENT: production
      DELIBRA_DATABASE_PATH: /app/data/delibra.db
    volumes:
      - delibra-data:/app/data
    restart: unless-stopped

volumes:
  delibra-data:
